# Installation

## On linux
```bash
# Install redis
sudo atp-get install redis
sudo yum install redis
# Start redis server
redis-server
# Default port is 6379

# Access redis, we open a new terminal
# Run the redis-cli binary
redis-cli

# this should connect us to redis (127.0.0.1:6379)
```
